# TODO

* Only a public header -> nplex-cpp.hpp
* allow ssl connections (see project tlsuv)
* Add bool transaction::exists(const char *key) ?
* Add size_t transaction::count(const char *pattern) ?
* Implement retry on TRY_LATER
* JSON output always in ndjson (remove indent support)
