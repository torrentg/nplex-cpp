# TODO

* NETWORK MSG = LEN + METADATA + DATA + CHECKSUM
* value_t::as_millis() convert from ISO-8601 string (instead of number)
* Only a public header -> nplex-cpp.hpp
* implement client.cpp
* allow ssl connections (see project tlsuv)
* add user permissions on login_response
* README: explain nplex purpose
* improve params_t class: set default values + setters returning ref to obj
* utility class to read config from file (optional)
* submit_tx_fut returns a future (attached to tx.promise)
* extends key_t from cstring_t adding validation (length, double-, ending/, utf8) on construct, key_part, key_preffix, key_suffix, etc.
* manage client_impl_t::async correctly

## Useful commands

* cloc --exclude-content="automatically generated" src test
* cppcheck --enable=all --inconclusive --std=c++17 --force --suppress=missingIncludeSystem --suppress=unusedFunction -Ideps -Isrc src &> removeme.txt
